[project]
name = "onboardlite"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alembic==1.14.1",
    "commonmark",
    "fastapi[standard]==0.115.8",
    "google-api-python-client",
    "google-auth",
    "jinja2",
    "joserfc",
    "passes-rs-py",
    "pydantic",
    "pydantic-settings",
    "python-keycloak",
    "pyyaml",
    "requests",
    "requests-oauthlib",
    "sentry-sdk",
    "sqlmodel==0.0.24",
    "stripe==11.4.1",
    "typing-extensions",
    "uvicorn",
]

[dependency-groups]
dev = [
    "httpx",
    "pre-commit",
    "pytest>=8.3.5",
    "semgrep",
    "virtualenv",
]

[tool.uv.sources]
passes-rs-py = { git = "https://github.com/jontyms/neo-passes-rs" }
